#include<stdio.h>
#include<string.h>

int main(){
    char str[50];  //bbabbaccddd
    scanf("%s",str);

    int s_length = strlen(str);

    char arr[s_length][s_length]; //['bb']

    for(int i=0;i<s_length;i++){
        int asc = str[i];

    }

    return 0;
}