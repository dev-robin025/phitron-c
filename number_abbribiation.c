#include <stdio.h>
#include <math.h>

float addAbbreviation(float val, char abb)
{
}

int main()
{
    long long int n;
    scanf("%lld", &n);

    float res;
    if (n >= 1e6)
    {
        res = n / 1e6;
    }

    return 0;
}